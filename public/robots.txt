# This file is used by web crawlers to understand how to crawl the site

User-agent: *  # Applies to all web crawlers
Allow: /       # Allow crawling of all content

# Disallow crawling of specific directories
Disallow: /api/          # Prevent crawlers from accessing API routes
Disallow: /private/      # Example: Prevent access to a 'private' directory
Disallow: /temporary/    # Example: Prevent access to a 'temporary' directory

# Allow specific pages even if their parent directories are disallowed
Allow: /public/page.html # Allow crawling of this specific page

# Sitemap Location
Sitemap: https://www.varunarealty.com/sitemap.xml # Update with your actual sitemap URL
